version: '{build}'
platform: x64
configuration: Release

build: off

build_script:
  - echo "Starting build..."
  - g++ main.cpp -o hello  # Linux не требует расширения .exe
  - echo "Build complete!"

test_script:
  - echo "Running the program..."
  - ./hello  # Запускаем программу
  - echo "Tests complete!"
